# Assets

Place your jME assets here (textures, models, sounds, etc.).

When loaded via the AssetManager, the classpath root is `src/main/resources`,
so assets can be addressed like:

- `Textures/` -> "Textures/MyTexture.png"
- `Models/` -> "Models/MyModel.j3o"
